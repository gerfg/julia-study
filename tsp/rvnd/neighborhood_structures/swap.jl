function swap(data, solution)
    println("--- Swap ---")
    # for i = 1:length(solution.customers)
        # for j = i:length(solution.customers)
            # i = 1
            # j = 2
            # if i != j 
            #     println("customers: ", solution.customers[i],",",solution.customers[j])
            #     swap_cost(data, solution, i, j)
            # end
        # end
    # end
end

# function swap_cost(data, solution, pos1, pos2)
#     if pos1 == 1 || pos2 == length(solution.customers)
#         println(pos1, " [1] ", pos2)
#     elseif pos2 == pos1 + 1
#         println(pos1, " [2] ", pos2)
#     else
#         println(pos1, " [3] ", pos2)
#     end
# end